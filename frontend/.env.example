# Frontend does not require API keys
# All credentials are configured in the backend automation-engine/.env

# Backend automatically handles:
# - APIFY_API_TOKEN
# - GOOGLE_SHEETS_CREDENTIALS
# - HEYGEN_API_KEY
# - SLACK_WEBHOOK_URL

# Frontend only needs to know the backend path (automatically resolved)
